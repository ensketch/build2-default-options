# Use Git with 'main' as default branch.
#
--vcs git,branch=main

# Use whitelisting by copying '.gitignore' template.
# Default '.gitignore' have already been cleared.
#
--post-hook "cp $options_dir$/project.gitignore @root@/.gitignore"

# Set up default Git hooks.
#
--post-hook "$options_dir$/git-hooks-init.sh"
--post-hook "$options_dir$/git-hooks-setup.sh"
#
--post-hook "cp $options_dir$/git-hooks-setup.sh ."

# Support Files
#
--post-hook "cp $options_dir$/AUTHORS.md @root@/."
--post-hook "cp $options_dir$/project.clang-format @root@/.clang-format"

# Manifest
#
# Remove comments and fix URL and e-mail address.
#
--post-hook "sed -i -e 's/^#.*$$//g' -e 's#^url: .*$$#url: https://github.com/ensketch/@name@#g' -e 's/^email: .*$$/email: ensketch@@mailbox.org/g' @root@/manifest"

--pre-hook "echo bdep-new-project"
